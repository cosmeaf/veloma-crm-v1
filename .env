########################
# Django / Core
########################
# Chave secreta para segurança do Django (mantenha segura em produção)
SECRET_KEY = 7ej54k1l+jt6tb7_t+lo-_t$0@4jhy!+$@ykx2rbd8(6=a2d+&

# Modo de depuração (False em produção)
DEBUG = True

# Hosts permitidos para servir a aplicação (CSV)
ALLOWED_HOSTS = 127.0.0.1,localhost,0.0.0.0,api.alvelos.com,185.44.65.171

# Configurações de CORS para desenvolvimento
CORS_ALLOW_ALL_ORIGINS = True
CORS_ALLOW_CREDENTIALS = True

# Origens confiáveis para CSRF (esquema + domínio, CSV)
CSRF_TRUSTED_ORIGINS = https://api.alvelos.com,http://api.alvelos.com,http://localhost:8000,http://127.0.0.1:8000,http://localhost:3000,http://127.0.0.1:3000

# Configuração para proxy/ingress que termina HTTPS (ex.: Nginx, Traefik)
USE_X_FORWARDED_PROTO = True

########################
# Banco de Dados
########################
# Configuração do banco de dados (opcional em desenvolvimento)
# Padrão: SQLite
# DATABASE_URL = sqlite:///db.sqlite3
# Exemplo com PostgreSQL:
# DATABASE_URL = postgres://user:pass@localhost:5432/veloma

########################
# Redis / Celery
########################
# URL do Redis para filas e cache
REDIS_URL = redis://127.0.0.1:6379/0

# Configurações do MinIO para armazenamento
STORAGE_BACKEND=minio
MINIO_ACCESS_KEY=admin-alvelos
MINIO_SECRET_KEY=alvelos2025
MINIO_BUCKET=finance-app
MINIO_SECURE=false

########################
# E-mail (Zoho EU)
########################
# Configuração para envio de e-mails em desenvolvimento
EMAIL_BACKEND = django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST = smtp.zoho.eu
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_USE_SSL = False
EMAIL_HOST_USER = suporte@velomacontabilidade.com
EMAIL_HOST_PASSWORD = Janeiro_32Janeiro_32
DEFAULT_FROM_EMAIL = suporte@velomacontabilidade.com
EMAIL_TIMEOUT = 30

# Para depuração em console (descomente se preferir):
# EMAIL_BACKEND = django.core.mail.backends.console.EmailBackend

########################
# URLs Públicas
########################
# URLs base para construção de links (ex.: e-mails)
PUBLIC_BASE_URL = https://api.alvelos.com
FRONTEND_BASE_URL = http://localhost:3000

# Caminhos para autenticação (ajuste conforme o frontend)
# AUTH_RESET_PATH = /reset-password
# AUTH_VERIFY_PATH = /verify-email